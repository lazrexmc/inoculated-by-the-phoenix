# DaVinci Resolve working project: copyrighted source audio (Tool — Fear Inoculum)
# plus large binary caches, backups, and gallery. Not source; never commit.
FearInoculum_Resolve/

# Local Claude Code settings
.claude/

# Python bytecode cache (Blender bpy scripts)
__pycache__/
*.pyc

# Music-sync pipeline: venv + heavy/generated audio outputs (scripts ARE tracked)
audio/.venv/
audio/stems/
audio/analysis/
audio/*.log

# Scratch / intermediates (kept off C: per drive policy)
_scratch/

# Rendered video / image-sequence outputs (large binaries; regenerate from the scripts)
blender/renders/

# OS / editor cruft
Thumbs.db
.DS_Store
desktop.ini
*.tmp
*~
